<h1 class="x_title">
  <i class="glyphicon glyphicon-leaf titleicon"></i>
  Novedades mensuales
</h1>

<%= link_to admin_new_register_path , :class =>"btn btn-dark pull-right" do%>
  <span class="glyphicon glyphicon-pencil"></span>
  Crear novedad mensual
<% end %>

<table id="datatable" class="display datatable" cellspacing="0" width="100%">
  <thead>
    <tr>
      <th>Fecha</th>
      <th>Acciones</th>
    </tr>
  </thead>

  <tbody>
    <% @registrations.each do |registration| %>
      <tr>
        <td><%= registration.registerDate %></td>
        <td>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<script>
$('#datatable').DataTable({
              "language": {
                "url": "../files/spanish"
            }
  // ajax: ...,
  // autoWidth: false,
  // pagingType: 'full_numbers',
  // processing: true,
  // serverSide: true,

  // Optional, if you want full pagination controls.
  // Check dataTables documentation to learn more about available options.
  // http://datatables.net/reference/option/pagingType
});
</script>
